FRONTEND_PORT=8080
BACKEND_PORT=8081

# Used by docker-compose for frontend → backend proxy.
# For standalone frontend (e.g. Render): set to your API URL or leave empty.
# BACKEND_URL=https://your-api.onrender.com
